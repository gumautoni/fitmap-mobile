# Contributing to FitMap

## Development workflow

The `main` branch represents the integrated and stable version of the project.

Development must be performed in short-lived branches created from an updated `main` branch.

Before starting a new task:

```bash
git switch main
git pull --ff-only